<Window x:Class="TaskManager.Wpf.LoginWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Login"
        Height="260" Width="380"
        WindowStartupLocation="CenterScreen">
    <Grid Margin="16">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <TextBlock Text="Email" />
        <TextBox x:Name="EmailBox" Grid.Row="1" Margin="0,6,0,12" Height="26"/>

        <TextBlock Text="Password" Grid.Row="2"/>
        <PasswordBox x:Name="PasswordBox" Grid.Row="3" Margin="0,6,0,12" Height="26"/>

        <StackPanel Grid.Row="4" Orientation="Horizontal" HorizontalAlignment="Left">
            <Button Content="Login" Width="100" Margin="0,0,10,0" Click="Login_Click"/>
            <Button Content="Register" Width="100" Click="Register_Click"/>
        </StackPanel>
    </Grid>
</Window>
